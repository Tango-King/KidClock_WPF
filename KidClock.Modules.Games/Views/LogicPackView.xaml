<UserControl x:Class="KidClock.Modules.Games.Views.LogicPackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:logic="clr-namespace:KidClock.Modules.Games.ViewModels.Logic"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance logic:LogicPackViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#34495E" Padding="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="益智游戏合集" FontSize="30" FontWeight="Bold" Foreground="White" Margin="0,0,16,0"/>
                    <TextBlock Text="（6-8岁）" FontSize="22" FontWeight="Bold" Foreground="#D6EAF8" VerticalAlignment="Bottom"/>
                </StackPanel>
                <Button Grid.Column="1" Content="刷新成绩" Command="{Binding RefreshCommand}" Padding="14,8" Margin="6"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Games}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8F9F9" CornerRadius="16" BorderThickness="2" BorderBrush="#D5DBDB" Padding="18" Margin="12" Width="320">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="30" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding BestText}" FontSize="18" Foreground="#2C3E50" TextWrapping="Wrap" Margin="0,0,0,12"/>
                                    <Button Content="开始" FontSize="22" FontWeight="Bold" Padding="12,8"
                                            Command="{Binding DataContext.NavigateToCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding ViewName}"
                                            Background="#2ECC71" Foreground="White" BorderThickness="0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Column="1" Background="#F4F6F7" CornerRadius="16" BorderThickness="2" BorderBrush="#D5DBDB" Padding="18" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="家长周报（近7天）" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding WeeklySummary}" FontSize="18" Foreground="#2C3E50" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

