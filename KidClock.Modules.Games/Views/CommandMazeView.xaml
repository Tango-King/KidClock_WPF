<UserControl x:Class="KidClock.Modules.Games.Views.CommandMazeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:logic="clr-namespace:KidClock.Modules.Games.ViewModels.Logic"
             xmlns:converters="clr-namespace:KidClock.Modules.Games.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance logic:CommandMazeViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <converters:StarsToTextConverter x:Key="StarsToText" />
        <converters:MazeCommandToTextConverter x:Key="MazeCommandToText" />

        <Style x:Key="MazeTileBorderStyle" TargetType="Border">
            <Setter Property="Width" Value="54"/>
            <Setter Property="Height" Value="54"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#95A5A6"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsWall}" Value="True">
                    <Setter Property="Background" Value="#2C3E50"/>
                    <Setter Property="BorderBrush" Value="#2C3E50"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsStart}" Value="True">
                    <Setter Property="Background" Value="#2ECC71"/>
                    <Setter Property="BorderBrush" Value="#27AE60"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsGoal}" Value="True">
                    <Setter Property="Background" Value="#E74C3C"/>
                    <Setter Property="BorderBrush" Value="#C0392B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPlayer}" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="BorderBrush" Value="#2E86DE"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MazeTileTextStyle" TargetType="TextBlock">
            <Setter Property="Text" Value=""/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsWall}" Value="True">
                    <Setter Property="Text" Value="墙"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsStart}" Value="True">
                    <Setter Property="Text" Value="起"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsGoal}" Value="True">
                    <Setter Property="Text" Value="终"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsPlayer}" Value="True">
                    <Setter Property="Text" Value="我"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F4F9FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#8E44AD" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="指令迷宫" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,0,16,0"/>
                    <TextBlock Text="{Binding BigPrompt}" FontSize="26" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="练习" Command="{Binding StartPracticeCommand}" Margin="6" Padding="10,6"/>
                    <Button Content="挑战90秒" Command="{Binding StartChallengeCommand}" Margin="6" Padding="10,6"/>
                    <Button Content="暂停" Command="{Binding PauseCommand}" Margin="6" Padding="10,6"/>
                    <Button Content="继续" Command="{Binding ResumeCommand}" Margin="6" Padding="10,6"/>
                    <Button Content="重置" Command="{Binding ResetCommand}" Margin="6" Padding="10,6"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F0E6F6" Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="难度:" FontSize="20" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding DifficultyTier}" FontSize="20" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock Text="倒计时:" FontSize="20" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding RemainingSeconds}" FontSize="20" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock Text="分数:" FontSize="20" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Score}" FontSize="20" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock Text="星级:" FontSize="20" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding StarRating, Converter={StaticResource StarsToText}}" FontSize="22" FontWeight="Bold" Foreground="#F1C40F"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderThickness="3" BorderBrush="#8E44AD" CornerRadius="16" Padding="16" Margin="0,0,16,0">
                <Grid x:Name="MazeGridContainer"/>
            </Border>

            <Border Grid.Column="1" Background="White" BorderThickness="3" BorderBrush="#8E44AD" CornerRadius="16" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="指令面板" FontSize="26" FontWeight="Bold" Foreground="#4A235A" HorizontalAlignment="Center"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
                        <Button Content="前进 ↑" Command="{Binding AddForwardCommand}" Margin="6" Padding="10,6"/>
                        <Button Content="左转 ↺" Command="{Binding AddLeftCommand}" Margin="6" Padding="10,6"/>
                        <Button Content="右转 ↻" Command="{Binding AddRightCommand}" Margin="6" Padding="10,6"/>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#FBF7FD" CornerRadius="12" Padding="12">
                        <StackPanel>
                            <TextBlock Text="你的指令：" FontSize="20" FontWeight="Bold" Foreground="#4A235A" Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding Program}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#8E44AD" CornerRadius="10" Padding="10" Margin="6">
                                            <TextBlock Text="{Binding Converter={StaticResource MazeCommandToText}}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                        <Button Content="撤销" Command="{Binding UndoCommand}" Margin="6" Padding="10,6"/>
                        <Button Content="清空" Command="{Binding ClearProgramCommand}" Margin="6" Padding="10,6"/>
                        <Button Content="运行" Command="{Binding RunCommand}" Margin="6" Padding="14,6" Background="#2ECC71" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#BBF8C291" CornerRadius="18" Padding="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Opacity" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowSuccessFeedback}" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.12"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" BeginTime="0:0:0.28" Duration="0:0:0.18"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="到达终点！" FontSize="44" FontWeight="Bold" Foreground="#1E8449"/>
            </Border>

            <Border Background="#BBD6EAF8" CornerRadius="18" Padding="18" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Opacity" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowHintFeedback}" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.12"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" BeginTime="0:0:0.28" Duration="0:0:0.18"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="再试一次，你一定行！" FontSize="28" FontWeight="Bold" Foreground="#1F3A93"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>

