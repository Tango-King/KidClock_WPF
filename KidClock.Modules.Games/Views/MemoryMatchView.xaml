<UserControl x:Class="KidClock.Modules.Games.Views.MemoryMatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:KidClock.Modules.Games.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:MemoryMatchViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:converters="clr-namespace:KidClock.Modules.Games.Converters">

    <UserControl.Resources>
        <converters:HourToAngleConverter x:Key="HourToAngleConverter" />
        <ControlTemplate x:Key="CardButtonTemplate" TargetType="{x:Type Button}">
            <Grid>
                <Border x:Name="BackBorder" Background="#FF9800" CornerRadius="10">
                    <TextBlock Text="?" FontSize="32" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Border x:Name="FrontBorder" Background="White" BorderBrush="#4CAF50" BorderThickness="2" CornerRadius="10" Visibility="Collapsed">
                    <Grid>
                        <TextBlock x:Name="DigitalText"
                                   Text="{Binding Hour, StringFormat='{}{0}:00'}"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>

                        <Canvas x:Name="AnalogCanvas"
                                Width="60"
                                Height="60"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <Ellipse Width="60" Height="60" Stroke="Black" StrokeThickness="2" />
                            <Line X1="30" Y1="30" X2="30" Y2="15" Stroke="Black" StrokeThickness="3">
                                <Line.RenderTransform>
                                    <RotateTransform Angle="{Binding Hour, Converter={StaticResource HourToAngleConverter}}" CenterX="30" CenterY="30"/>
                                </Line.RenderTransform>
                            </Line>
                            <Line X1="30" Y1="30" X2="30" Y2="5" Stroke="Black" StrokeThickness="2"/>
                        </Canvas>
                    </Grid>
                </Border>

                <Border x:Name="MatchedOverlay" Background="#80FFFFFF" CornerRadius="10" Visibility="Collapsed"/>
            </Grid>

            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding IsFlipped}" Value="True">
                    <Setter TargetName="BackBorder" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="FrontBorder" Property="Visibility" Value="Visible"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding IsDigital}" Value="True">
                    <Setter TargetName="DigitalText" Property="Visibility" Value="Visible"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding IsDigital}" Value="False">
                    <Setter TargetName="AnalogCanvas" Property="Visibility" Value="Visible"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding IsMatched}" Value="True">
                    <Setter TargetName="MatchedOverlay" Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid Background="#E0F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#00BCD4" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="记忆翻牌" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                
                <TextBlock Text="难度:" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
                <Button Content="4x4" Command="{Binding StartGameCommand}" CommandParameter="4" Margin="5" Padding="10,5"/>
                <Button Content="6x6" Command="{Binding StartGameCommand}" CommandParameter="6" Margin="5" Padding="10,5"/>
                <Button Content="8x8" Command="{Binding StartGameCommand}" CommandParameter="8" Margin="5" Padding="10,5"/>
                
                <TextBlock Text="{Binding GameStatus}" Margin="20,0,0,0" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="分数: " Margin="20,0,0,0" VerticalAlignment="Center" Foreground="White"/>
                <TextBlock Text="{Binding Score}" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" x:Name="GameGridContainer" Margin="20">
        </Grid>
    </Grid>
</UserControl>
