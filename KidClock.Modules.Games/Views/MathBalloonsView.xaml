<UserControl x:Class="KidClock.Modules.Games.Views.MathBalloonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:KidClock.Modules.Games.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:MathBalloonsViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Background="#E1F5FE">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#03A9F4" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="趣味算术" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                
                <TextBlock Text="难度:" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
                <ComboBox ItemsSource="{Binding DifficultyOptions}" SelectedItem="{Binding SelectedDifficulty}" 
                          Width="150" FontSize="18" Margin="0,0,20,0" IsEditable="False"/>
                
                <Button Content="开始游戏" Command="{Binding StartGameCommand}" Margin="5" Padding="10,5"/>
                <Button Content="停止" Command="{Binding StopGameCommand}" Background="#FF5252" Foreground="White" Margin="5" Padding="10,5"/>
                
                <TextBlock Text="分数: " Margin="20,0,0,0" VerticalAlignment="Center" Foreground="White"/>
                <TextBlock Text="{Binding Score}" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Game Area -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Balloons}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas ClipToBounds="True"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}" />
                    <Setter Property="Canvas.Top" Value="{Binding Y}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.BalloonClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                            CommandParameter="{Binding}"
                            Width="80" Height="100" Background="Transparent" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Path Data="M 40,100 C 10,100 0,60 0,40 C 0,20 20,0 40,0 C 60,0 80,20 80,40 C 80,60 70,100 40,100" 
                                          Fill="{Binding Color}" Stroke="White" StrokeThickness="2">
                                        <Path.Effect>
                                            <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.3"/>
                                        </Path.Effect>
                                    </Path>
                                    <TextBlock Text="{Binding Value}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20" Foreground="White">
                                        <TextBlock.Effect>
                                            <DropShadowEffect ShadowDepth="1" BlurRadius="2" Color="Black" Opacity="0.8"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Question Display -->
        <Border Grid.Row="2" Background="#81D4FA" Padding="20">
            <TextBlock Text="{Binding CurrentQuestion}" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
